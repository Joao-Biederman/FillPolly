{
  "name": "fillpolly",
  "version": "1.0.0",
  "description": "",
  "main": "colors.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "nodemon --exec 'npm run build' --watch src && http-server ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "nodemon": "^3.1.7",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "toastr": "^2.1.4"
  }
}
